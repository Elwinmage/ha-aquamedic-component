repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: check-json
      - id: trailing-whitespace

  - repo: https://github.com/pre-commit/mirrors-prettier
    rev: v4.0.0-alpha.8
    hooks:
      - id: prettier
        types: [json]

  - repo: local
    hooks:
      - id: hassfest
        name: hassfest
        entry: env PYTHONPATH=/usr/src/homeassistant python3 -m script.hassfest
        language: system
        args: ["--integration-path", "custom_components/aquamedic"]
        pass_filenames: false
        always_run: true

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.10
    hooks:
      - id: ruff-check
        args: [--fix]
        files: ^(custom_components/.*\.py|scripts/.*\.py|tests/.*\.py)$
      - id: ruff-format
        files: ^(custom_components/.*\.py|scripts/.*\.py|tests/.*\.py)$

  - repo: local
    hooks:
      - id: check_translation
        name: Check Entities translations
        entry: python scripts/check_translation.py
        language: python
        additional_dependencies:
          - colorama>=0.4.6
        pass_filenames: false

      - id: pyright
        exclude: ^tests/
        name: pyright
        entry: pyright
        language: python
        args: [--project, pyrightconfig.json]
        additional_dependencies:
          - pyright>=1.1.390
          - pytest-homeassistant-custom-component>=0.13.301
          - colorama>=0.4.6
        pass_filenames: false

      - id: pytest
        name: pytest (-q)
        entry: pytest -q --cov=custom_components.aquamedic --cov-report=xml:reports/coverage.xml --cov-report=term-missing --cov-report=html:reports/htmlcov
        language: python
        always_run: true
        pass_filenames: false
        additional_dependencies:
          - pytest>=8
          - pytest-asyncio>=1.3.0
          - pytest-cov>=7.0.0
          - pytest-homeassistant-custom-component>=0.13.301
